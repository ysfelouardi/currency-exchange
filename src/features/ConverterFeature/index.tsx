import { memo } from "react";
import { PageTitle } from "../../components/common";

function ConverterFeature() {
  return <PageTitle>I want to convert</PageTitle>;
}

export default memo(ConverterFeature);
